services:
  honeycomb:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - honeycomb-data:/app/.data

volumes:
  honeycomb-data:
